{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "schedule": ["before 6am on monday"],
  "prConcurrentLimit": 5,
  "labels": ["dependencies"],
  "packageRules": [
    {
      "description": "Group CI image bumps into a single weekly MR. This is the whole job for an R package: there is no CRAN manager, and DESCRIPTION declares version floors resolved against current CRAN at install time, so nothing else here is pinned. Replaces the old Dependabot `github-actions` group, which kept the r-lib/actions pins current.",
      "matchManagers": ["gitlabci", "gitlabci-include"],
      "groupName": "ci images",
      "commitMessagePrefix": "ci:"
    }
  ]
}
